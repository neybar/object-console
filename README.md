Install object console:

* requires perl 5.24 (plenv install 5.24.0 && plenv install-cpanm && plenv rehash)
* cpanm Data::Debug IO::Prompter Try::Tiny Types::Standard
* ./console TestObject

('tis a silly project... proof of concept)
